/**
 * Inbox route - serves the same content as /agent
 *
 * This route exists to provide a clean URL (/inbox) with custom
 * Open Graph metadata for the AI Agent Inbox feature.
 * Metadata is defined in layout.tsx.
 */
export { default } from '../agent/page';
